#ifndef __shell_sort__
#define __shell_sort__
#include"lista.h"

/*
 * ordena os elementos(distancia dos postos de saude) a partir de shell sort
 * necessita da quantidade postos e suas distancias
 * nao retorna nada em especifico
 */
int shellSort(Dist  distancia[], int k, int quant_posto);

#endif